{
  "name": "microservices",
  "version": "0.1.0",
  "scripts": {
    "docker": "npm run import-web --prefix srvs/main && docker-compose up",
    "docker-freshen-npm": "npm run import-web --prefix srvs/main && docker-compose up --build -V",
    "dive:login-api": "docker run --rm -it -v /var/run/docker.sock:/var/run/docker.sock wagoodman/dive:latest 201911a_loginapi:latest",
    "dive:main": "docker run --rm -it -v /var/run/docker.sock:/var/run/docker.sock wagoodman/dive:latest 201911a_main:latest"
  },
  "author": "Don Alvarez",
  "license": "ISC"
}
