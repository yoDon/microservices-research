{
  "name": "microservices",
  "version": "0.1.0",
  "scripts": {
    "docker": "npm run import-web --prefix main && docker-compose up",
    "docker-freshen-npm": "npm run import-web --prefix main && docker-compose up --build -V"
  },
  "author": "Don Alvarez",
  "license": "ISC"
}
